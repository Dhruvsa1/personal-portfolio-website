import DiscoveryDashboard from '@/components/discovery/DiscoveryDashboard';

export const metadata = {
  title: 'Discovery Project — D.D.',
  description: 'ECE 1100 Discovery Project: Time Token Planner + Wearable Focus Sensor',
};

export default function DiscoveryPage() {
  return <DiscoveryDashboard />;
}
